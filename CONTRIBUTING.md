# 贡献指南

感谢您对 Deep Learning Tutorial 项目的关注！我们欢迎各种形式的贡献。

## 如何贡献

### 报告问题

如果您发现 bug 或有功能建议，请：

1. 检查 [Issues](https://github.com/Kimi-ming/Deep-Learning-Tutorial/issues) 是否已有相关讨论
2. 如果没有，创建新 Issue，并提供：
   - 清晰的问题描述
   - 重现步骤（如适用）
   - 期望行为
   - 实际行为
   - 环境信息（Python版本、操作系统等）

### 提交代码

1. **Fork 项目**
   ```bash
   # 在 GitHub 上 fork 项目
   # 然后克隆你的 fork
   git clone https://github.com/YOUR_USERNAME/Deep-Learning-Tutorial.git
   cd Deep-Learning-Tutorial
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **设置开发环境**
   ```bash
   # 安装开发依赖
   pip install -r requirements-dev.txt

   # 或使用 make 命令
   make install-dev
   ```

4. **进行修改**
   - 遵循项目代码规范
   - 添加必要的测试
   - 更新相关文档

5. **运行测试和代码检查**
   ```bash
   # 格式化代码
   make format

   # 运行代码检查
   make lint

   # 运行测试（当测试可用时）
   make test
   ```

6. **提交变更**
   ```bash
   git add .
   git commit -m "类型: 简洁的变更描述

   详细描述你的变更内容和原因
   "
   ```

   提交信息格式：
   - `feat:` 新功能
   - `fix:` Bug 修复
   - `docs:` 文档更新
   - `style:` 代码格式（不影响代码运行的变动）
   - `refactor:` 重构
   - `test:` 测试相关
   - `chore:` 构建过程或辅助工具的变动

7. **推送到 GitHub**
   ```bash
   git push origin feature/your-feature-name
   ```

8. **创建 Pull Request**
   - 在 GitHub 上创建 Pull Request
   - 填写 PR 描述模板
   - 等待代码审查

## 开发规范

### 代码风格

本项目使用以下工具保证代码质量：

- **Black**: 代码格式化
  - 行长度：100 字符
  - 配置：见 `pyproject.toml`

- **Ruff**: 代码检查
  - 规则：E, F, I, W
  - 配置：见 `pyproject.toml`

运行命令：
```bash
# 自动格式化代码
make format

# 检查代码问题
make lint
```

### Python 编码规范

1. **命名约定**
   - 类名：`PascalCase`（如 `Perceptron`, `MLP`）
   - 函数名：`snake_case`（如 `forward_pass`, `calculate_loss`）
   - 常量：`UPPER_SNAKE_CASE`（如 `MAX_ITERATIONS`, `LEARNING_RATE`）

2. **文档字符串**
   ```python
   def function_name(param1, param2):
       """
       函数的简短描述

       详细说明（如需要）

       Args:
           param1 (type): 参数1的描述
           param2 (type): 参数2的描述

       Returns:
           type: 返回值的描述

       Example:
           >>> result = function_name(1, 2)
           >>> print(result)
           3
       """
       pass
   ```

3. **注释**
   - 关键算法步骤添加中文注释
   - 复杂数学公式添加公式说明
   - 避免不必要的注释

4. **类型提示**（推荐但非必需）
   ```python
   def add(a: int, b: int) -> int:
       return a + b
   ```

### 测试规范

1. **单元测试**
   - 测试文件放在 `tests/` 目录
   - 测试文件名：`test_*.py`
   - 测试函数名：`test_*`

2. **测试编写**
   ```python
   def test_perceptron_and_gate():
       """测试感知机实现 AND 门"""
       p = Perceptron(input_size=2)
       X = [[0, 0], [0, 1], [1, 0], [1, 1]]
       y = [0, 0, 0, 1]

       # 训练
       for epoch in range(100):
           for xi, yi in zip(X, y):
               p.train(xi, yi)

       # 测试
       for xi, yi in zip(X, y):
           pred = p.predict(xi)
           assert pred == yi, f"Expected {yi}, got {pred}"
   ```

3. **运行测试**
   ```bash
   # 运行所有测试
   make test

   # 或使用 pytest 直接运行
   pytest tests/

   # 运行特定测试文件
   pytest tests/test_fundamentals.py

   # 查看覆盖率
   pytest --cov=deep_learning tests/
   ```

## 项目结构

了解项目结构有助于您的贡献：

```
Deep-Learning-Tutorial/
├── deep_learning/         # [未来] 主包目录
├── tests/                 # [未来] 测试目录
├── examples/              # [未来] 示例代码
├── docs/                  # 文档
├── main.py                # CLI 入口
├── deep_learning_*.py     # 当前教学模块
├── requirements.txt       # 生产依赖
├── requirements-dev.txt   # 开发依赖
├── pyproject.toml         # 项目配置
├── Makefile               # 开发命令
└── README.md              # 项目说明
```

详细架构设计请参阅 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

## 开发工作流

### 日常开发

```bash
# 1. 更新主分支
git checkout main
git pull origin main

# 2. 创建功能分支
git checkout -b feature/your-feature

# 3. 进行开发
# 编写代码...

# 4. 运行代码检查
make format  # 格式化代码
make lint    # 检查代码

# 5. 运行测试（如有）
make test

# 6. 提交变更
git add .
git commit -m "feat: 添加新功能"

# 7. 推送分支
git push origin feature/your-feature

# 8. 创建 Pull Request
```

### Pull Request 检查清单

提交 PR 前请确认：

- [ ] 代码已通过 `make format` 格式化
- [ ] 代码已通过 `make lint` 检查
- [ ] 所有测试已通过（如适用）
- [ ] 已添加必要的文档
- [ ] 已更新 CHANGELOG.md（如需要）
- [ ] PR 描述清晰，说明了变更内容和原因
- [ ] 关联了相关 Issue（如适用）

## 贡献类型

### 代码贡献

- 实现新的算法或架构
- 优化现有实现
- 修复 bug
- 添加测试

### 文档贡献

- 改进现有文档
- 添加示例代码
- 翻译文档
- 修正错别字或描述不清的地方

### 其他贡献

- 提出改进建议
- 报告 bug
- 参与 Issue 讨论
- 帮助回答问题

## 行为准则

参与本项目时，请：

- 尊重其他贡献者
- 保持友善和建设性的讨论
- 欢迎新手提问和贡献
- 专注于技术问题

## 获取帮助

如果您在贡献过程中遇到问题：

1. 查阅 [FAQ.md](FAQ.md)
2. 搜索现有 [Issues](https://github.com/Kimi-ming/Deep-Learning-Tutorial/issues)
3. 创建新 Issue 寻求帮助
4. 在 PR 中 @ 维护者

## 致谢

感谢所有为本项目做出贡献的人！

您的每一个贡献，无论大小，都让这个项目变得更好。

---

再次感谢您的贡献！ 🎉
